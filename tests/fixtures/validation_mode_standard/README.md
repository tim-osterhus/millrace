validation fixture for the shadowed default autonomous mode

- Extends `base`.
- Shadows `mode.default_autonomous` at the workspace registry layer.
- Rebinds the packaged autonomous mode to the packaged `execution.standard` loop.
